---
alwaysApply: true
---

# Passport UI Library - Project Overview

This is **passport-ui**, a modern React UI component library published on npm. It provides a comprehensive set of components and utilities for building apps blazingly fast.

## Library Architecture

- **Package Name**: `passport-ui` (npm)
- **Target Framework**: React 18+
- **Styling**: Tailwind CSS v4 with shadcn/ui components
- **Motion**: Motion primitives
- **Theme System**: Built-in dark/light theme support

## Key Components Categories

1. **Layout Containers** ([src/layouts/](mdc:src/layouts/))
   - Higher-level components for common page patterns
   - Includes PageLayout, ContentContainer, SidebarContainer etc.

2. **Providers** ([src/providers/](mdc:src/providers/))
   - Providers for theme, etc.

3. **UI Components** ([src/components/](mdc:src/components/))
   - Atomic components like Button, Card, Badge, etc.
   - Built on Radix UI primitives
   - Fully accessible and customizable

4. **Hooks** ([src/hooks/](mdc:src/hooks/))
   - Custom React hooks for common use cases
   - Includes useMobile, useScroll, etc.

5. **Composables** ([src/composables/](mdc:src/composables/))
   - Higher-level components for common composables
   - Usually made with combination of components and utilities
   - Includes EmptyState, ThemeButton, ThemeToggle, etc.

6. **Motion Primitives** ([src/motion-primitives/](mdc:src/motion-primitives/))
   - Motion primitives for animations using Motion
   - Includes AnimatedBackground, BlurIn, SequentialReveal, TypewriterText, etc.

## Entry Points

- **Main Export**: [src/index.ts](mdc:src/index.ts) - All component exports
- **Styles**: [src/styles.css](mdc:src/styles.css) - Core CSS with Tailwind theme

## Consumer Setup Requirements

Consuming projects must:
1. Install Tailwind CSS v4 and configure postcss
2. Import the styles: `import 'passport-ui/styles.css'`
3. Set up proper postcss config for Tailwind
4. Configure theme provider for dark/light mode support